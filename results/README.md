# Results

`manuscript_reference_metrics.csv` contains the principal aggregate values reported in the manuscript and is provided as a quick cross-check.

New executions should be written to `results/generated/` (ignored by Git by default unless you intentionally decide to archive a frozen result set).

Because the secondary analyses use reduced-complexity configurations for sensitivity analysis, their values should not be mixed with the primary benchmark when reporting the main paper results.
